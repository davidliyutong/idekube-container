name: Docker Build

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    build:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                arch: [amd64, arm64]
        steps:
            - name: Checkout repository
              uses: actions/checkout@v2

            - name: Log in to Docker Hub
              uses: docker/login-action@v2
              with:
                  username: ${{ secrets.DOCKER_USERNAME }}
                  password: ${{ secrets.DOCKER_PASSWORD }}

            - name: Install vcs
              run: sudo apt-get install -y python3-pip && pip3 install vcstool

            - name: Run make build_all
              run: |
                  APT_MIRROR=archive.ubuntu.com; make build_all

    build-latest:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                arch: [amd64, arm64]
        steps:
            - name: Checkout repository
              uses: actions/checkout@v2

            - name: Log in to Docker Hub
              uses: docker/login-action@v2
              with:
                  username: ${{ secrets.DOCKER_USERNAME }}
                  password: ${{ secrets.DOCKER_PASSWORD }}

            - name: Install vcs
              run: sudo apt-get install -y python3-pip && pip3 install vcstool

            - name: Set GIT_TAG to latest
              run: echo "GIT_TAG=latest" >> $GITHUB_ENV

            - name: Run make build_all with latest tag
              run: |
                  APT_MIRROR=archive.ubuntu.com; make build_all
